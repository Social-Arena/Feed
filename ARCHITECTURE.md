# Feed Package Architecture

## High-Level Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     USER APPLICATION                         │
│                    import feed                               │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────┴──────────────────────────────────────┐
│                   feed/__init__.py                           │
│           (Public API & Convenience Functions)               │
│                                                              │
│  • create_tweet()                                           │
│  • simulate_twitter()                                       │
└───┬─────────────────┬─────────────────┬────────────────────┘
    │                 │                 │
    ▼                 ▼                 ▼
┌─────────┐    ┌─────────────┐   ┌──────────────┐
│ MODELS  │    │  UTILITIES  │   │  SIMULATION  │
└─────────┘    └─────────────┘   └──────────────┘
```

---

## Detailed Architecture

```
╔═══════════════════════════════════════════════════════════════╗
║                     FEED PACKAGE                              ║
╚═══════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────┐
│                    PUBLIC API LAYER                           │
│                   feed/__init__.py                            │
├───────────────────────────────────────────────────────────────┤
│  Exports:                                                     │
│  • All models (Feed, FeedType, PublicMetrics, etc.)          │
│  • All utilities (FeedManager, extract_entities, etc.)       │
│  • All simulation classes (TwitterSimulator, etc.)           │
│                                                               │
│  Convenience Functions:                                       │
│  • create_tweet(text, author_id, **kwargs) → Feed           │
│  • simulate_twitter(num_tweets, num_users) → (tweets, stats)│
└───────────────────────────────────────────────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼

┌─────────────────┐  ┌──────────────────┐  ┌────────────────────┐
│  MODELS LAYER   │  │ UTILITIES LAYER  │  │ SIMULATION LAYER   │
│  feed/models/   │  │  feed/utils/     │  │  feed/simulation/  │
└─────────────────┘  └──────────────────┘  └────────────────────┘
```

---

## Models Layer (`feed/models/`)

```
┌────────────────────────────────────────────────────────┐
│                     MODELS                             │
├────────────────────────────────────────────────────────┤
│                                                        │
│  feed.py                                              │
│  ┌──────────────────────────────────────────────┐   │
│  │ @dataclass                                    │   │
│  │ class Feed:                                   │   │
│  │   • id: str                                   │   │
│  │   • text: str                                 │   │
│  │   • author_id: str                            │   │
│  │   • created_at: str                           │   │
│  │   • feed_type: FeedType                       │   │
│  │   • conversation_id: Optional[str]            │   │
│  │   • referenced_feeds: List[ReferencedFeed]    │   │
│  │   • entities: Optional[Entities]              │   │
│  │   • public_metrics: PublicMetrics             │   │
│  │   + to_dict() → dict                          │   │
│  │   + from_dict(data) → Feed                    │   │
│  └──────────────────────────────────────────────┘   │
│                                                        │
│  entities.py                                          │
│  ┌──────────────────────────────────────────────┐   │
│  │ • HashtagEntity(start, end, tag)             │   │
│  │ • MentionEntity(start, end, username, id)    │   │
│  │ • UrlEntity(start, end, url, ...)            │   │
│  │ • Entities(hashtags, mentions, urls)         │   │
│  └──────────────────────────────────────────────┘   │
│                                                        │
│  metrics.py                                           │
│  ┌──────────────────────────────────────────────┐   │
│  │ • PublicMetrics                               │   │
│  │   - like_count                                │   │
│  │   - retweet_count                             │   │
│  │   - reply_count                               │   │
│  │   - quote_count                               │   │
│  │   - bookmark_count                            │   │
│  │   - impression_count                          │   │
│  └──────────────────────────────────────────────┘   │
│                                                        │
│  references.py                                        │
│  ┌──────────────────────────────────────────────┐   │
│  │ • ReferencedFeedType(Enum)                    │   │
│  │   - REPLIED_TO                                │   │
│  │   - QUOTED                                    │   │
│  │   - RETWEETED                                 │   │
│  │ • ReferencedFeed(type, id)                    │   │
│  └──────────────────────────────────────────────┘   │
│                                                        │
│  user.py                                              │
│  ┌──────────────────────────────────────────────┐   │
│  │ • User(id, username, name, ...)               │   │
│  └──────────────────────────────────────────────┘   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## Utilities Layer (`feed/utils/`)

```
┌────────────────────────────────────────────────────────┐
│                    UTILITIES                           │
├────────────────────────────────────────────────────────┤
│                                                        │
│  manager.py                                           │
│  ┌──────────────────────────────────────────────┐   │
│  │ class FeedManager:                            │   │
│  │   + __init__(storage_dir)                     │   │
│  │   + create_feed(text, author_id, ...) → Feed │   │
│  │   + save_feed(feed) → filepath                │   │
│  │   + load_feed(filepath) → Feed                │   │
│  │   + load_all_feeds() → List[Feed]             │   │
│  │   + search_feeds(...) → List[Feed]            │   │
│  └──────────────────────────────────────────────┘   │
│                                                        │
│  entities.py                                          │
│  ┌──────────────────────────────────────────────┐   │
│  │ def extract_entities(text) → Entities:        │   │
│  │   • Uses regex to find:                       │   │
│  │     - Hashtags (#word)                        │   │
│  │     - Mentions (@username)                    │   │
│  │     - URLs (https://...)                      │   │
│  └──────────────────────────────────────────────┘   │
│                                                        │
│  generators.py                                        │
│  ┌──────────────────────────────────────────────┐   │
│  │ def generate_feed_id() → str                  │   │
│  │ def create_sample_user() → User               │   │
│  └──────────────────────────────────────────────┘   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## Simulation Layer (`feed/simulation/`)

```
┌────────────────────────────────────────────────────────────────┐
│                        SIMULATION                              │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  simulator.py (Main Orchestrator)                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ class TwitterSimulator:                               │   │
│  │   + __init__(config: SimulationConfig)                │   │
│  │   + generate_users(count) → List[UserBehavior]        │   │
│  │   + create_tweet(user, timestamp) → Feed              │   │
│  │   + simulate(num_tweets, duration) → (feeds, stats)   │   │
│  │   + save_results() → dict                             │   │
│  └──────────────────────────────────────────────────────┘   │
│         │                │                  │                  │
│         │                │                  │                  │
│         ▼                ▼                  ▼                  │
│  ┌──────────┐   ┌──────────────┐   ┌─────────────────┐      │
│  │ behavior │   │   content    │   │   engagement    │      │
│  └──────────┘   └──────────────┘   └─────────────────┘      │
│                                                                │
│  config.py                                                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ @dataclass                                            │   │
│  │ class SimulationConfig:                               │   │
│  │   • num_users: int = 100                              │   │
│  │   • num_tweets: int = 1000                            │   │
│  │   • duration_hours: int = 24                          │   │
│  │   • peak_hours: List[int]                             │   │
│  │   • like_rate: float = 0.15                           │   │
│  │   • retweet_rate: float = 0.03                        │   │
│  │   • reply_rate: float = 0.05                          │   │
│  │   • quote_rate: float = 0.02                          │   │
│  │   • reply_probability: float = 0.20                   │   │
│  │   • quote_probability: float = 0.10                   │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                                │
│  behavior.py                                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ @dataclass                                            │   │
│  │ class UserBehavior:                                   │   │
│  │   • user_id: str                                      │   │
│  │   • activity_level: str                               │   │
│  │     - "low" (33% of users)                            │   │
│  │     - "medium" (50% of users)                         │   │
│  │     - "high" (15% of users)                           │   │
│  │     - "influencer" (2% of users)                      │   │
│  │   • posting_frequency: float                          │   │
│  │   • engagement_probability: float                     │   │
│  │   • follower_count: int                               │   │
│  │   • hashtag_usage_rate: float                         │   │
│  │   • mention_rate: float                               │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                                │
│  content.py                                                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ class ContentGenerator:                               │   │
│  │   • TOPICS: Dict[str, List[str]]                      │   │
│  │     - technology, news, lifestyle                     │   │
│  │     - entertainment, sports                           │   │
│  │   • PLACEHOLDERS: Dict[str, List[str]]                │   │
│  │   + generate_tweet(topic) → str                       │   │
│  │   + generate_hashtags(count) → List[str]              │   │
│  │   + generate_thread(topic, length) → List[str]        │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                                │
│  engagement.py                                                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ class EngagementCalculator:                           │   │
│  │   + calculate_engagement_score() → float              │   │
│  │     Factors:                                          │   │
│  │     - Author influence (+0.3 for influencers)         │   │
│  │     - Time decay (exponential over 24h)               │   │
│  │     - Hashtag bonus (+0.03 per hashtag)               │   │
│  │   + generate_metrics() → PublicMetrics                │   │
│  │     Formula: reach × rate × random(0.5-1.5)           │   │
│  │   + simulate_viral_growth() → PublicMetrics           │   │
│  │     5% chance, logarithmic growth                     │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    SIMULATION FLOW                          │
└─────────────────────────────────────────────────────────────┘

1. Configuration
   ┌──────────────┐
   │ User creates │
   │ config       │
   └──────┬───────┘
          │
          ▼
   ┌──────────────────┐
   │ SimulationConfig │
   │ • num_users: 100 │
   │ • num_tweets: 500│
   └──────┬───────────┘
          │
          ▼

2. User Generation
   ┌─────────────────────┐
   │ TwitterSimulator    │
   │ .generate_users()   │
   └──────┬──────────────┘
          │
          ▼
   ┌─────────────────────┐
   │ UserBehavior × 100  │
   │ • 2 influencers     │
   │ • 15 high activity  │
   │ • 50 medium         │
   │ • 33 low            │
   └──────┬──────────────┘
          │
          ▼

3. Tweet Creation Loop
   ┌─────────────────────────────────────────┐
   │ for i in range(num_tweets):             │
   │   1. Select user (weighted by activity) │
   │   2. Generate content                   │
   │   3. Add entities                       │
   │   4. Handle references (reply/quote)    │
   │   5. Calculate engagement               │
   │   6. Add viral growth (5% chance)       │
   │   7. Save tweet                         │
   └─────────────┬───────────────────────────┘
                 │
                 ▼
          ┌─────────────┐
          │ Feed × 500  │
          └─────────────┘

4. Statistics Collection
   ┌─────────────────────┐
   │ Aggregate stats:    │
   │ • total_tweets      │
   │ • total_replies     │
   │ • total_quotes      │
   │ • total_engagement  │
   │ • viral_tweets      │
   │ • by_hour          │
   └─────────────────────┘
```

---

## Usage Patterns

### Pattern 1: Quick Tweet Creation

```
User Code                    Feed Package
──────────                  ──────────────

import feed                 feed/__init__.py
                                │
tweet = feed.create_tweet() │   create_tweet()
                                │
                                ▼
                            FeedManager
                                │
                                ▼
                            Feed.from_dict()
                                │
                                ▼
                            Return Feed object
```

### Pattern 2: Simulation

```
User Code                    Feed Package
──────────                  ──────────────

import feed                 feed/__init__.py
                                │
simulator = TwitterSimulator()  │
                                │
                                ▼
tweets, stats =             TwitterSimulator
simulator.simulate()            │
                                ├──> generate_users()
                                │       UserBehavior
                                │
                                ├──> create_tweet()
                                │       ContentGenerator
                                │       EngagementCalculator
                                │
                                ├──> FeedManager.create_feed()
                                │       Feed model
                                │
                                └──> Collect statistics
                                        │
                                        ▼
                                    Return (tweets, stats)
```

---

## Key Design Principles

### 1. **Separation of Concerns**
- Models: Data structures only
- Utils: Helper functions
- Simulation: Business logic

### 2. **Single Responsibility**
- Each file has one clear purpose
- Each class has one job
- Each function does one thing

### 3. **Dependency Flow**
```
Simulation → Utils → Models
     ↓         ↓        ↓
  (uses)   (uses)   (pure data)
```

### 4. **Extensibility**
- Easy to add new feed types
- Easy to add new user behaviors
- Easy to add new content templates
- Easy to add new engagement models

### 5. **Testability**
- Pure functions where possible
- Mockable dependencies
- Clear interfaces
- Isolated components

---

## Summary

The Feed package follows a **clean, layered architecture** with:

✅ **Models** - Pure data structures
✅ **Utilities** - Helper functions and management
✅ **Simulation** - Complex behavior modeling
✅ **Public API** - Simple, user-friendly interface

This design ensures:
- 🔧 Easy to use
- 🧩 Easy to extend
- 🧪 Easy to test
- 📚 Easy to understand
- 🚀 Production-ready

---

*For usage examples, see `QUICKSTART.md` or `examples/`*

